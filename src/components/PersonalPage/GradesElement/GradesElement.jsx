import React from 'react';

const GradesElement = (grades) => {
    const gradesObj = grades;
    return (
        <div>
            {gradesObj}
        </div>
    );
};

export default GradesElement;